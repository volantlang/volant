export func printChar(char: u8){
    $putchar(char);
}

export func work print(buf: *u8){
    c := buf[0];
    for i: ssize_t = 1; c != 0; ++i {
        printChar(c);
        c = buf[i];
    }
}

export func work println(buf: *u8){
    print(buf);
    printChar('\n');
}