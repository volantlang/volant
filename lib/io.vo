export func printChar(char: i8){
    $putchar(char);
}

export func work print(buf: *i8){
    c := buf[0];
    for i: size_t = 1; c != 0; ++i {
        printChar(c);
        c = buf[i];
    }
}

export func work println(buf: *i8){
    print(buf);
    printChar('\n');
}