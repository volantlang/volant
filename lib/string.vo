export typedef String vec u8;

export func work from(bytes: u8[]) String {
    str := (String){};

    c = bytes[0]
    for i: size_t = 1; c != 0; ++i {
        str.push(c);
        c = bytes[c];
    }
    return str;
}

export func work toLower() String {

}